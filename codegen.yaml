# copied from the graphql code generator quick start guide
schema: https://api.spacex.land/graphql # schema data location
documents: './src/**/*.graphql' # client side gql
# output and list of plugins
generates:
  ./src/app/spacex-api.ts:
    plugins:
      - typescript
      - typescript-operations